# Claude Code Instructions for {{PROJECT_NAME}}

Shared project rules live in [AGENTS.md](AGENTS.md). Read that first.

## Claude-specific

{{CLAUDE_SPECIFIC_BULLETS}}

{{SKILLS_POINTER}}

{{MCP_POINTER}}

---
Placeholders:
  {{PROJECT_NAME}}              — repo / product name
  {{CLAUDE_SPECIFIC_BULLETS}}   — only things that differ from AGENTS.md:
                                    - slash commands this project uses
                                    - permission / hook expectations
                                    - model preferences if any
  {{SKILLS_POINTER}}            — e.g. "Project skills live under `.claude/skills/`."
  {{MCP_POINTER}}               — e.g. "MCP servers are configured in `.mcp.json`."

Keep this file short (< ~50 lines). If it grows, move content to AGENTS.md or a focused doc.
